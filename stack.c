#include <stdio.h>
#include <assert.h>

#define HALFMAX 10
#define MAX 20

// stack follows only LIFO order
// queue follows only FIFO order, Need to implement it 
// using LIFO

int stack[MAX]; 
int queue1[HALFMAX], queue2[HALFMAX];

int main(int argc, char **argv)
{
    return 0;
}
